services:
  redis:
    image: "redis:alpine3.8"

  cloud-server:
    image: "server-cloud"
    ports:
      - "6670:6670"


  redis-server:
    image: "server-redis"
    ports:
      - "6671:6671"
    links:
      - redis

